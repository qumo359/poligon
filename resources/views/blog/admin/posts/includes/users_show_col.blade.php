    <table class="table"> {{-- Используем Bootstrap классы для таблицы (если Bootstrap подключен) --}}
        <thead>
        <tr>
            <th>ID</th>
            <th>Имя</th>
            <th>Email</th>
            <th>Дата регистрации</th>
            <th>Действия</th> {{-- Можно добавить столбцы с действиями, например, редактирование, удаление --}}
        </tr>
        </thead>
        <tbody>
        @foreach($users as $user) {{-- Перебираем коллекцию пользователей, переданную из контроллера --}}
        <tr>
            <td>{{ $user->id }}</td>
            <td>{{ $user->name }}</td>
            <td>{{ $user->email }}</td>
            <td>{{ $user->created_at }}</td>
            <td>
                {{-- Ссылки на действия (пока пустые, можно добавить редактирование, удаление и т.д.) --}}
                <a href="#">Редактировать</a> |
                <a href="#">Удалить</a>
            </td>
        </tr>
        @endforeach
        </tbody>
    </table>
